# Use sum() to add numbers.

numbers = [10, 20, 30, 40]

total = sum(numbers)

print(total)


# use it with comprehension:

numbers = [1, 2, 3, 4, 5]

total_squares = sum(n * n for n in numbers)

print(total_squares)