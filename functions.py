def myFunction(x,y):  # definition;
    c = x + y  # indentation has to be followed
    print(c)  # no need actually after using return
    return c   # for returning output via function calling to some other file or variable

# myFunction(10,20) # function calling

result = myFunction(8,9) # function calling with parameters/arguments

print("my result is ", result)

#need to understand concept of return